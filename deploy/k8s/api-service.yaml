apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app: reliability-lab
    component: api
spec:
  type: ClusterIP
  selector:
    app: reliability-lab
    component: api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
